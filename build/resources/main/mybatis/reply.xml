<?xml version="1.0" encoding="UTF-8" ?> 

<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.rentcar.reply.mapper.ReplyMapper">
<delete id="bdelete" parameterType="int">
DELETE FROM reply
WHERE listno = #{listno}
</delete>

<insert id="create" parameterType="com.rentcar.reply.model.ReplyDTO">
     insert into reply(content, regdate, id, listno)
    values(#{content}, sysdate(), #{id}, #{listno}) 
</insert>

<delete id="delete" parameterType="int" >
delete from REPLY
where rnum = #{rnum}
</delete>

<select id="list" parameterType="Map" resultType="com.rentcar.reply.model.ReplyDTO">
select rnum, content, regdate, id, listno
from REPLY
where listno = ${listno}
order by rnum DESC
limit #{sno}, #{eno};
</select>

<select id="read" parameterType="int" resultType="com.rentcar.reply.model.ReplyDTO">
  select * from reply
    where rnum = #{rnum}
</select>

<select id="total" parameterType="int" resultType="int">
 select count(*) from reply
    where listno=#{listno}
</select>

<update id="update" parameterType="com.rentcar.reply.model.ReplyDTO">
update reply
set content = #{content}
where rnum = #{rnum}
</update>

</mapper>